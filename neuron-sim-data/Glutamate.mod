TITLE Mod file for component: Component(id=Glutamate type=expTwoSynapse)

COMMENT

    This NEURON file has been generated by org.neuroml.export (see https://github.com/NeuroML/org.neuroml.export)
         org.neuroml.export  v1.11.0
         org.neuroml.model   v1.11.0
         jLEMS               v0.12.0

ENDCOMMENT

NEURON {
    POINT_PROCESS Glutamate
    RANGE tauRise                           : parameter
    RANGE tauDecay                          : parameter
    RANGE peakTime                          : parameter
    RANGE waveformFactor                    : parameter
    RANGE gbase                             : parameter
    RANGE erev                              : parameter
    RANGE g                                 : exposure
    RANGE i                                 : exposure
    
    
    NONSPECIFIC_CURRENT i 
    
}

UNITS {
    
    (nA) = (nanoamp)
    (uA) = (microamp)
    (mA) = (milliamp)
    (A) = (amp)
    (mV) = (millivolt)
    (mS) = (millisiemens)
    (uS) = (microsiemens)
    (nF) = (nanofarad)
    (molar) = (1/liter)
    (kHz) = (kilohertz)
    (mM) = (millimolar)
    (um) = (micrometer)
    (umol) = (micromole)
    (pC) = (picocoulomb)
    (S) = (siemens)
    
}

PARAMETER {
    
    tauRise = 1 (ms)                       : was: 0.001 (time)
    tauDecay = 2 (ms)                      : was: 0.002 (time)
    peakTime = 1.3862944 (ms)              : was: 0.0013862943611198907 (time)
    waveformFactor = 4                     : was: 4.0 (none)
    gbase = 1.0E-5 (uS)                    : was: 1.0000000000000001E-11 (conductance)
    erev = 0 (mV)                          : was: 0.0 (voltage)
}

ASSIGNED {
    ? Standard Assigned variables with baseSynapse
    v (mV)
    celsius (degC)
    temperature (K)
    g (uS)                                  : derived variable
    i (nA)                                  : derived variable
    rate_A (/ms)
    rate_B (/ms)
    
}

STATE {
    A  : dimension: none
    B  : dimension: none
    
}

INITIAL {
    temperature = celsius + 273.15
    
    rates()
    rates() ? To ensure correct initialisation.
    
    A = 0
    
    B = 0
    
}

BREAKPOINT {
    
    SOLVE states METHOD cnexp
    
    
}

NET_RECEIVE(weight) {
    
    : paramMappings . : {Glutamate={A=A, B=B, tauRise=tauRise, tauDecay=tauDecay, peakTime=peakTime, waveformFactor=waveformFactor, gbase=gbase, erev=erev, g=g, i=i}, Glutamate_notes={}}
    : state_discontinuity(A, A  + (weight *   waveformFactor  )) : From Glutamate
    A = A  + (weight *   waveformFactor  ) : From Glutamate
    
    : paramMappings . : {Glutamate={A=A, B=B, tauRise=tauRise, tauDecay=tauDecay, peakTime=peakTime, waveformFactor=waveformFactor, gbase=gbase, erev=erev, g=g, i=i}, Glutamate_notes={}}
    : state_discontinuity(B, B  + (weight *   waveformFactor  )) : From Glutamate
    B = B  + (weight *   waveformFactor  ) : From Glutamate
    
}

DERIVATIVE states {
    rates()
    A' = rate_A 
    B' = rate_B 
    
}

PROCEDURE rates() {
    
    g = gbase  * (  B   -   A  ) ? evaluable
    i = -1 * g  * (  erev   - v) ? evaluable
    rate_A = -  A   /  tauRise ? Note units of all quantities used here need to be consistent!
    rate_B = -  B   /  tauDecay ? Note units of all quantities used here need to be consistent!
    
     
    
}

